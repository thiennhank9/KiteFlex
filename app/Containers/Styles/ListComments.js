import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    input: {
        borderRadius: 3,
        borderWidth: 0.5,
        borderColor: 'gray',
        marginLeft: 40,
        //flex: 1
    }
})