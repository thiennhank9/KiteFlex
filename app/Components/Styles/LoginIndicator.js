import {StyleSheet} from 'react-native';
export default styles = StyleSheet.create({
    container: {
        flexDirection: 'column',
    },

    textLoading: {
        fontSize: 20,
        fontWeight: 'bold',
        color: 'chocolate'
    }
})